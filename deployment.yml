---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bemypet
spec:
  replicas: 5
  selector:
    matchLabels:
      name: bemypt
  template:
    metadata:
      labels:
        name: bemypt
    spec:
      containers:
      - name: application
        image: gnvshanessa/matching-service:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 7777